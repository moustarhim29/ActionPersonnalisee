name: Test de mon action

on:
  push:
    branches:
      - main

jobs:
  test-action:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Dire bonjour avec mon action
        uses: moustarhim29/ActionPersonnalisee@main
        with:
          nom: "moustarhim"
          langue: "es"